export interface InvestmentHolding {
    id: string;

    investmentId: string;

    quantity: number;

    averageCost: number;

    currentPrice: number;

    currentValue: number;

    asOfDate: string;

    createdAt: string;

    updatedAt: string;
}
