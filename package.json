{
  "name": "financeos",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.15.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
